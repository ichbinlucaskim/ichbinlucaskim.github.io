export default function Tag({ children }) {
  return (
    <span className="rounded-full border border-line bg-canvas px-2.5 py-0.5 text-[12px] font-medium text-muted">
      {children}
    </span>
  )
}
